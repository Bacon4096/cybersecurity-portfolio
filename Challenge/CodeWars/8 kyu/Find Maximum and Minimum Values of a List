def minimum(arr):
    min_val = arr[0]
    
    for num in arr:
        if num < min_val:
            min_val = num
    return min_val

def maximum(arr):
    max_val = arr[0]
    
    for num in arr:
        if num > max_val:
            max_val = num
    return max_val
